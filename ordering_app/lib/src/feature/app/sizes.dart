import 'package:flutter/material.dart';

class AppSizes {
  static const double appBarHeight = 56;
  static const double datePickerHeight = 44;
  static const double inputLeadingIconSize = 20;
  static const double inputBorderWidth = 0.5;
  static const double addAddressTabInputHeight = 44;
  static const double selecteAddressTabInputHeight = 37;
  static const double nextStepButtonHeight = 52;
  static const double selectedAddressBoxIconsSize = 20;
  static const double selecteAddresBoxHeight = 80;
  static const double searchInputItemHeight = 60;

  static const EdgeInsets orderingPageContentPadding = EdgeInsets.symmetric(horizontal: 20);
  static const EdgeInsets datePickerPadding = EdgeInsets.all(12);
  static const EdgeInsets selectedAddresBoxPadding = EdgeInsets.symmetric(horizontal: 16, vertical: 20);

  static const BorderRadius inputBorderRadius = BorderRadius.all(Radius.circular(8));
  static const BorderRadius tabBarSelectedLabelBorderRadius = BorderRadius.all(Radius.circular(36));
  static const BorderRadius nextStepButtonBorderRadius = BorderRadius.all(Radius.circular(50));
}
